info:
  name: Trigger Notifications
  type: http
  seq: 1
  tags:
    - Notifications

http:
  method: POST
  url: "{{alerta_doador_backend}}/notifications/send-alerts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/notifications/send-alerts"
      method: POST
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
